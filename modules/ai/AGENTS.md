## Tool Availability

If a binary/program is not installed locally, use `nix-shell -p ${tool} --command ${command}` to run the tool you need
for troubleshooting or one-off tasks. The program does not need to be on `PATH`.
